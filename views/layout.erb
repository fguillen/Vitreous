<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= @title %>
    </title>
    <link rel='stylesheet' href='/style.css' type='text/css' />
    <link rel="icon" type="image/png" href="http://example.com/myicon.png" />
    <meta charset="utf-8" />
  </head>
  <body>
    <div id="container">
      <div id="menu">
        <ul>
          <li><a href="/">Home</a></li>
          <% @collections.each_value do |collection| %>
            <% if collection[:slug] != '_root' %>
            <li>
              <%= collection[:title] %>
              <ul>
                <% collection[:items].each_value do |item| %>
                  <li><a href="/<%= collection[:slug] %>/<%= item[:slug] %>"><%= item[:title] %></a></li>
                <% end %>
              </ul>
            </li>
            <% end %>
          <% end %>
        </ul>
        <p>Super Designer - professional works</p>
        <p>Built with Vitreous</p>
      </div>
  
      <div id="content">
        <%= yield %>
      </div>
    </div>
  </body>
</html>